import { ComponentPipe } from './filter-Name.pipe';

describe('ComponentPipe', () => {
  it('create an instance', () => {
    const pipe = new ComponentPipe();
    expect(pipe).toBeTruthy();
  });
});
